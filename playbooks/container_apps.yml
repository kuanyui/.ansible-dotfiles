---
- name: Install container apps (launchers + completions) via `roles/container_apps`
  hosts: localhost
  connection: local
  gather_facts: true
  vars:
    setup_container_apps: true  # dedicated entry point - bypass the host-level opt-in gate
  roles:
    - container_apps
